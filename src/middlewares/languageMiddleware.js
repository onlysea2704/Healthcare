/*import i18n from 'i18n';

export const languageMiddleware = (req, res, next) => {
    let lang = req.cookies.lang || 'en'; // Mặc định là tiếng Anh
    i18n.setLocale(req, lang);
    res.locals.lang = lang; // Cho phép dùng biến lang trong file EJS
    next();
};*/

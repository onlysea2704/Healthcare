<!DOCTYPE html>
<html lang="vi">
<head>
    <title> Đăng ký tài khoản mới với Doctors Care</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap 4.3, Jquery 3.3, font-awesome 4.7, slick-slider-->
    <script src="/components/jquery/dist/jquery.min.js"></script>
    <link rel="stylesheet" href="/components/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/components/font-awesome/css/font-awesome.min.css">
    <script src="/components/bootstrap/js/bootstrap.min.js"></script>
</head>
<body>
<div class="login">
    <div class="container lg-body">
        <div class="container d-flex flex-row">
            <div class="col-6 lg-image-background">
            </div>
            <div class="col-6 login-form">
                <span class="text-login"> Đăng ký tài khoản mới </span>
                <% if(locals.hasErrors){ %>
                    <p style="color: red">Đã có lỗi xảy ra, vui lòng kiểm tra lại thông tin của bạn</p>
                <% } %>
                <form method="POST" action="/register">
                    <div class="form-group">
                        <label for="name">Họ tên:</label>
                        <input type="text" class="form-control" id="name" name="name"
                               value="<% if (locals.oldData) { %> <%= locals.oldData.name %> <% } %>"
                               placeholder="Họ tên...">
                    </div>
                    <div class="form-group">
                        <label for="email">Email *:</label>
                        <input type="email" class="form-control" id="email" name="email"
                               placeholder="Email ..."
                               value="<% if (locals.oldData) { %> <%= locals.oldData.email %> <% } %>">
                        <% if (locals.errEmail) { %>
                            <small class="form-text invalid-feedback"><%= locals.errEmail %></small>
                        <% } %>
                    </div>
                    <div class="form-group">
                        <label for="password">Mật khẩu *:</label>
                        <input type="password" class="form-control" id="password" name="password"
                               placeholder="Mật khẩu...">
                        <% if (locals.errPassword) { %>
                            <small class="form-text invalid-feedback"><%= locals.errPassword %></small>
                        <% } %>
                    </div>
                    <div class="form-group">
                        <label for="rg-password-again"> Xác nhận mật khẩu*:</label>
                        <input type="password" class="form-control" id="rg-password-again" name="passwordConfirm"
                               placeholder="Confirm password...">
                        <% if (locals.errPasswordConfirm) { %>
                            <small class="form-text invalid-feedback"><%= locals.errPasswordConfirm %></small>
                        <% } %>
                    </div>

                    <div>
                        <span>(Thông tin có dấu * là bắt buộc)</span>
                    </div>
                    <button type="submit" class="btn btn-primary btn-register">Đăng kí</button>
                </form>
                <br>
                <hr>
                <span>Bạn đã có tài khoản? <a class="lg-register" href="/login">Quay lại trang đăng nhập</a></span>
            </div>
        </div>
    </div>
</div>
</body>
</html>
